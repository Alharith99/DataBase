# Place your exported APEX application here

After building the app in Oracle APEX:

1. App Builder → Export / Import → Export
2. Download the application export (`.sql`)
3. Save it in this folder, e.g. `f100_al_noor_hospital.sql`

Submit this file together with the `sql/` folder from the project root.
